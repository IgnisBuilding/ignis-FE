import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Remove all experimental.turbo configuration
  // The default configuration works fine for Tailwind CSS
}

export default nextConfig